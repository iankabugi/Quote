import { HighlightHighestDirective } from './highlight-highest.directive';

describe('HighlightHighestDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHighestDirective();
    expect(directive).toBeTruthy();
  });
});
